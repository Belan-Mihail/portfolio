{
  "name": "my-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd my-portfolio-backend && npm --force install  && npm run build  && cd ../my-porfolio && npm install && npm run build",
    "start": "TG_ALLOW_MIXED=ALLOW  node my-portfolio-backend/build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
